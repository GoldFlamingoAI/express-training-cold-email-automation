List all available slash commands in .claude/commands/ for this project.

Read the directory and produce a table with:
- Command name (e.g. /status)
- One-line description of what it does

Then add a note: "Run any command by typing /command-name in Claude Code."
